<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - TechShop</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Estilos rápidos para a nova área de imagem */
        .product-image-container {
            flex: 1;
            max-width: 500px;
            background-color: #f9f9f9;
            border-radius: 8px;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 400px;
            border: 1px solid #eee;
        }

        .product-image-container img {
            width: 100%;
            height: auto;
            object-fit: cover;
        }

        .no-image {
            text-align: center;
            color: #999;
        }

        .admin-section {
            background-color: #fff4e5;
            padding: 15px;
            border-radius: 8px;
            border: 1px solid #ffe0b2;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">
            <a href="/" style="color: white; text-decoration: none;"><h2>TechShop</h2></a>
        </div>
        <nav>
            <a href="/">Voltar ao Catálogo</a>
            <% if (locals.user) { %>
                <span style="color: white; margin-right: 10px;">Olá, <strong><%= user.username %></strong></span>
                <a href="/logout">Sair</a>
            <% } else { %>
                <a href="/login">Login</a>
            <% } %>
        </nav>
    </header>

    <div class="container">
        <div class="product-detail-container" style="display: flex; gap: 40px; margin-top: 40px;">
            
            <div class="product-image-container">
                <% if (product.image_url) { %>
                    <img src="<%= product.image_url %>" alt="Imagem de <%= product.name %>">
                <% } else { %>
                    <div class="no-image">
                        <span style="font-size: 5rem;">📦</span>
                        <p>Nenhuma imagem cadastrada</p>
                    </div>
                <% } %>
            </div>

            <div class="product-info" style="flex: 1;">
                <h1><%= product.name %></h1>
                <p class="category" style="color: #666; text-transform: uppercase; font-size: 0.8rem;">Categoria: Hardware</p>
                
                <h2 class="price" style="color: #2ecc71; font-size: 2rem; margin: 20px 0;">
                    R$ <%= Number(product.price).toLocaleString('pt-BR', { minimumFractionDigits: 2 }) %>
                </h2>

                <p class="stock">
                    Estoque: <strong><%= product.stock %> unidades</strong>
                </p>
                
                <div class="description-box" style="margin: 30px 0; line-height: 1.6;">
                    <h3>Descrição do Produto</h3>
                    <p><%= product.description %></p>
                </div>

                <button class="btn-buy" style="width: 100%; padding: 15px; font-size: 1.2rem; cursor: pointer;">
                    Adicionar ao Carrinho
                </button>

                <% if (locals.user && user.role === 'seller') { %>
                    <div class="admin-section">
                        <h4 style="margin-top: 0;">Gerenciamento de Produto</h4>
                        <div style="display: flex; gap: 10px;">
                            <a href="/admin/edit-product/<%= product.id %>" class="btn-edit" style="background: #f39c12; color: white; padding: 10px 20px; text-decoration: none; border-radius: 4px;">
                                Editar Produto
                            </a>

                            <form action="/products/delete/<%= product.id %>" method="POST" onsubmit="return confirm('CUIDADO: Tem certeza que deseja excluir permanentemente?')">
                                <button type="submit" style="background: #e74c3c; color: white; padding: 10px 20px; border: none; border-radius: 4px; cursor: pointer;">
                                    Excluir Produto
                                </button>
                            </form>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>